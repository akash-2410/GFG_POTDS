 int[] kLargest(int[] arr, int n, int k) {
      int i,j;
        int A[]=new int[k];
       Arrays.sort(arr);
       for(i=0;i<k;i++)
       {
          A[i]=arr[arr.length-i-1];
       }
       return A;
        
    }
